The structure of the .dat file is as follows:
{'<roll_number>':{<data>},'<roll_number>':{<data>}...}

file init_records.py initlises the marks.dat
file print_current_records.py prints the current records
file update_records.py prints the current records then updates if needed.


